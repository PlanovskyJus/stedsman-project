# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#


default: &default
  adapter: postgresql
  encoding: unicode
  adapter: postgresql
  encoding: unicode
  pool: 5
  username: postgres
  password:
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: stedsman_development

test:
  <<: *default
  database: stedsman_test

production:
  <<: *default
  database: stedsman_production
  username: stedsman
  password: <%= ENV['POOPER_DATABASE_PASSWORD'] %>
